#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n, k ,t, xl, yd, xr, yu;

    cin >> n >> k >> t >> xl >> yd >> xr >> yu;
    vector<vector<pair<int, int>>> all_user(n);
    for (int i = 0; i < n; i++)
    {

    }
    return 0;
}